// Shared between client components and server actions, so this file must
// NOT import 'server-only'.

/** Default duration applied to a new timed event when only a start time is given. */
export const DEFAULT_EVENT_DURATION_MINUTES = 60;
